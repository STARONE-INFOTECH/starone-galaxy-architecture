name: Reusable Governance Pipeline

on:
  workflow_call:

jobs:
  governance-info:
    name: Reusable Governance Workflow
    runs-on: ubuntu-latest

    steps:
      - name: Governance Pipeline
        run: |
          echo "Reusable governance workflow executed"