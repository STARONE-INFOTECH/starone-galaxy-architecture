name: Architecture Review

on:
  pull_request:

jobs:
  architecture-check:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: ADR Validation
        run: echo "Validate ADR"

      - name: Traceability Validation
        run: echo "Validate RTM"